import 'package:flutter/material.dart';

class AppColor {
  static const Color primarycolor = Colors.orangeAccent;
  static const Color darkcolor = Color.fromRGBO(252, 202, 36, 2);
  static const Color transperant = Colors.transparent;
}
